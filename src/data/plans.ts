import type { LngLat } from '../types';

/**
 * 북한 2군단의 "계획선" — 실제로는 그어지지 못한 유령 화살표.
 * 개전 계획: 춘천·홍천을 당일 돌파한 뒤 수원 방면으로 깊숙이 우회,
 * 서울 정면에서 밀려 내려오는 국군 주력의 퇴로를 차단·포위섬멸한다.
 * 6사단의 3일 지연으로 이 선은 지도 위에 존재하지 않게 됐다.
 */
export interface PlanArrow {
  id: string;
  label: string;
  path: LngLat[]; // 남서쪽으로 화면 밖까지 뻗는다 — 의도된 프레이밍
}

export const planArrows: PlanArrow[] = [
  {
    id: 'plan-2div-seoul-east',
    label: 'D+1 계획: 서울 동측 → 수원 우회',
    path: [
      [127.734, 37.885], // 춘천
      [127.640, 37.760],
      [127.500, 37.600],
      [127.330, 37.430],
      [127.180, 37.300], // 수원 방면 (화면 밖)
    ],
  },
  {
    id: 'plan-12div-icheon',
    label: 'D+2 계획: 홍천 → 이천 방면 차단',
    path: [
      [127.889, 37.697], // 홍천
      [127.780, 37.610],
      [127.600, 37.460],
      [127.430, 37.300], // 이천 방면 (화면 밖)
    ],
  },
];

/** 계획 실패가 확정적으로 드러나는 시점 (가래목·큰말고개 이후) */
export const PLAN_FAILED_FROM = '1950-06-28';

export const planNote =
  '지도의 반투명 점선은 북한 2군단의 "계획선"이다 — 춘천을 당일 돌파해 수원 방면으로 우회, ' +
  '국군 주력의 퇴로를 끊는 포위선. 6사단이 3일을 버티면서 이 선은 끝내 그어지지 못했다.';
